import 'package:flutter/material.dart';

class Game2DScreen extends StatelessWidget {
  const Game2DScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(child: Text("Game 2D Screen"))
    );
  }
}
