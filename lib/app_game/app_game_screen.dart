import 'package:flutter/material.dart';

class AppGameScreen extends StatelessWidget {
  const AppGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: SafeArea(
        child: Text("App Game Screen")
      )
    );
  }
}
